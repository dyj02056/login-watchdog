# 로그인 워치독 — 구현 전 리서치 노트

> 작성일: 2026-09-02
> 근거 문서:
> - `login_watchdog_supabase.md` (종합 기획서)
> - `login_watchdog_briefing_supabase.html` (팀 브리핑 자료)
>
> **주의**: 이 문서는 구현이 아니라 분석입니다. 아래 내용을 바탕으로 실제 코드를 작성하기 전에 반드시 "구현 전 질문" 섹션의 답을 확정하세요.
>
> **변경 사항(2026-09-02)**: 원 기획서·브리핑 문서는 알림 채널로 텔레그램 봇을 명시하지만, 팀 결정에 따라 **Slack(Incoming Webhook)** 으로 대체합니다. 아래 분석은 이 변경을 반영해 갱신했습니다.

---

## 0. 저장소 현재 상태 (가장 먼저 확인할 사실)

`C:\Users\dyj02\OneDrive\Documents\projects_jsw\login-watchdog` 는 **완전히 빈 디렉터리**입니다.

- git 커밋 없음 (`master` 브랜치에 커밋 0개)
- 파일/폴더 0개 — `app.py`, `db.py`, `templates/` 등 기획서 8장에 나온 폴더 구조가 **아직 하나도 만들어지지 않음**

즉, 이번 요청은 "기존 코드가 어떻게 동작하는지"를 분석하는 게 아니라, **기획서가 요구하는 목표 아키텍처를 파일 단위로 해석하고, 처음부터 구현할 때 부딪힐 위험·모호점을 미리 정리하는 작업**입니다. 아래 "현재 구조 설명"은 실제 코드가 아니라 문서상 설계를 code-level로 재구성한 것입니다.

---

## 1. 목표 아키텍처 — 설계 요약

### 1-1. 전체 흐름 (기획서 3장 다이어그램 기준)

```
사용자/공격자 → /login (Flask) → Supabase(login_attempts 테이블) 기록
                                        ↓
                          detector.py: 같은 IP, 60초 내 실패 횟수 조회
                                        ↓
                     5회 이하 → 정상, 대시보드에 표시
                     5회 초과 → soar.py: 해당 IP 로그인 시도 강제 거부(잠금)
                                        ↓
                          alert.py(Slack 웹훅) + dashboard.html(폴링) 동시 통지
                                        ↓
                     관리자: 대시보드 버튼으로 즉시 해제 / 5분 후 자동 해제
```

### 1-2. 데이터 계층

- DB: Supabase(PostgreSQL 관리형, 무료 티어 — 500MB, 월 API 요청 약 5만 건)
- 테이블은 **`login_attempts` 단 하나**만 명시됨:
  ```sql
  create table login_attempts (
    id bigint generated always as identity primary key,
    ip_address text not null,
    username text not null,
    success boolean not null,
    attempted_at timestamptz not null default now()
  );
  ```
- 접속 정보는 `.env`의 `SUPABASE_URL`, `SUPABASE_KEY` (anon public key)로 관리, `.gitignore` 필수, `.env.example`만 커밋.
- 로컬 DB 파일(SQLite 등)은 사용하지 않음 — 팀원 4명이 각자 컴퓨터에서 동일한 클라우드 DB를 공유하는 것이 이 프로젝트의 핵심 설계 이유.

### 1-3. 판정/대응 로직 (기획서 6장 예시 코드 기준)

- `count_recent_failures(ip, window_seconds=60)`: 최근 60초 내 해당 IP의 `success=False` 레코드 개수를 Supabase에서 `count="exact"`로 조회
- `detector.py`는 이 반환값이 5를 넘는지만 판단
- `soar.py`가 판정 결과에 따라 "잠금"을 실행 — 단, **잠금 상태를 저장하는 별도 테이블/컬럼이 스키마에 없음** (2-3절, 4-2절에서 위험/질문으로 다룸)

---

## 2. 기능 동작 흐름 정리 (요청별 4가지 산출물 기준)

### 기능 1 — 로그인 시도 기록
- 트리거: `/login`에 POST로 아이디/비밀번호 제출
- 동작: 서버가 시각·IP·성공 여부를 Supabase `login_attempts`에 insert
- 특이사항: 실제 서비스 계정이 아니라 **팀이 만든 가짜 로그인 페이지 전용** — 감시 대상이 자체 제작 테스트 시스템으로 한정됨

### 기능 2 — 실패 횟수 카운트
- 트리거: 매 로그인 시도(혹은 대시보드 폴링) 시점
- 동작: 같은 IP가 최근 60초 내 몇 번 실패했는지 Supabase 조회로 실시간 계산

### 기능 3 — 자동 잠금
- 트리거: 실패 횟수가 5회를 "초과"하는 순간
- 동작: 해당 IP의 이후 로그인 시도를 서버가 강제 거부("잠긴 계정입니다" 응답), Slack 알림 발송(잠금 순간에만, 알림 피로 방지 목적)

### 기능 4 — 자동/수동 해제
- 자동: 5분 경과 후 해제
- 수동: 관리자가 대시보드 버튼 클릭 시 즉시 해제
- **문제**: 5분이라는 잠금 지속시간과 60초라는 탐지 윈도우가 서로 다른 시간 단위인데, 이를 구분해서 저장하는 상태(잠금 시작 시각, 잠금 여부 플래그 등)가 스키마에 없음 → 위험요소 3-2, 질문 4-2에서 상세

### 웹 대시보드
- `/dashboard` 접속 시 2~3초 간격 폴링으로 Supabase 테이블 재조회
- 표시 항목: 실시간 로그 테이블, 실패 횟수 카드, 잠금 상태, 즉시 해제 버튼
- 스트레치: Supabase Realtime 구독으로 폴링 대체 가능(선택사항)

### 공격 시뮬레이션 (스트레치, `scripts/bruteforce_sim.py`)
- `http://127.0.0.1:5000/login`에만 POST 요청 5회 순차 전송 후, 6번째 요청으로 "잠긴 계정" 문구가 응답에 포함되는지 검증
- 원칙: 팀이 소유한 로컬 서버만 대상으로 하며, 실제 서비스/타인 계정에는 절대 사용 금지 (문서에 명시된 안전 원칙)

---

## 3. 관련 파일 목록 (기획서 8장 폴더 구조 — 전부 "신규 생성" 대상)

| 파일 | 역할 | 담당 | 비고 |
|---|---|---|---|
| `app.py` | Flask 진입점 — `/login`, `/dashboard`, `/api/status` 라우트 | A·B | 아직 미생성 |
| `db.py` | Supabase 클라이언트 생성, `log_attempt()`, `count_recent_failures()` | A·B | 예시 코드 6장에 전체 구현 존재 |
| `detector.py` | 60초 내 5회 실패 판정 | A·B | `db.py`의 카운트 함수를 감싸는 얇은 로직으로 추정 |
| `soar.py` | 판정에 따른 자동 잠금/해제 실행 | C | 잠금 상태 저장 방식 미정 (질문 4-2) |
| `alert.py` | Slack 알림 발송 | C | Slack Incoming Webhook + `requests` 사용 |
| `requirements.txt` | flask, supabase, python-dotenv, requests 등 | 공통 | |
| `.env` / `.env.example` | Supabase 접속 키 | 공통 | `.env`는 반드시 `.gitignore` |
| `.gitignore` | `.env`, `__pycache__/`, `*.pyc` 등 | 공통 | |
| `templates/login.html` | 가짜 로그인 페이지 | A·B | Flask 규칙상 폴더명 고정 |
| `templates/dashboard.html` | 대시보드 화면 | D | Flask 규칙상 폴더명 고정 |
| `static/css/dashboard.css` | 대시보드 스타일 | D | |
| `static/js/dashboard.js` | 2~3초 폴링 갱신 로직 | D | |
| `scripts/bruteforce_sim.py` | 공격 시뮬레이션(선택) | A·B | 예시 코드 10장/07장에 전체 구현 존재 |
| `scripts/daily_report.py` | AI 로그 요약 리포트(선택) | C | LLM 프롬프트 연동, 세부 사양 없음 |
| `docs/architecture.png`, `demo.gif`, `before-after.png`, `scenario.md` | 발표/README용 시각자료 | D | |
| `README.md` | 프로젝트 소개 | D | |

**수정 가능 파일 + 이유**: 현재 저장소가 비어 있으므로 "수정"이라는 개념 자체가 성립하지 않습니다. 위 표의 모든 파일이 **신규 생성 대상**입니다. 굳이 우선순위를 매기면:
- `db.py`, `.env.example`, `.gitignore`가 가장 먼저 생성돼야 함 — 나머지 모든 모듈(`app.py`, `detector.py`, `soar.py`)이 Supabase 연결에 의존하기 때문
- `app.py`는 `templates/login.html`이 없으면 렌더링이 실패하므로 두 파일을 함께 만들어야 함 (Flask는 `templates/`, `static/` 폴더명을 하드코딩으로 요구)

---

## 4. 기존 규칙 (문서상 명시된 컨벤션 — 아직 코드 선례는 없음)

코드가 하나도 없으므로 "기존 규칙"은 실제로는 **기획서가 제시하는 최초 컨벤션 후보**입니다. 향후 파일을 늘려갈 때 이 기준을 그대로 따르는 것이 합리적입니다.

- **네이밍**
  - 파일: 소문자 스네이크케이스 모듈명 (`db.py`, `detector.py`, `soar.py`, `alert.py`)
  - 함수: 동사+목적어 스네이크케이스 (`log_attempt`, `count_recent_failures`)
  - Flask 폴더명(`templates/`, `static/`)은 프레임워크 강제 규칙이므로 임의 변경 불가
  - DB 컬럼: snake_case (`ip_address`, `attempted_at`) — Supabase/PostgreSQL 관례와 일치

- **상태 관리**
  - 서버 측에 별도 인메모리 상태(dict, 캐시 등)를 두지 않고, **Supabase 테이블 조회 자체가 상태의 유일한 원천(source of truth)** — 6장 예시 코드가 매 요청마다 DB를 재조회하는 방식
  - 프런트엔드는 "바닐라 JS(fetch, setInterval)" 폴링만 사용 — 프레임워크(React 등) 도입 계획 없음, 클라이언트 측 상태 관리 라이브러리도 없음

- **API 호출**
  - Supabase는 `supabase-py` 클라이언트를 통해 REST 방식으로 호출 (`.table().select()/.insert()/.eq()/.gte()/.execute()` 체이닝)
  - Slack은 Incoming Webhook URL에 `requests.post()`로 JSON(`{"text": "..."}`) 전송 — 원 기획서의 텔레그램 봇 API 호출 방식과 동일한 패턴이므로 대체 난이도는 낮음(질문 4-3)
  - `/api/status` 엔드포인트가 언급되지만 요청/응답 스펙(JSON 필드명 등)은 정의되어 있지 않음

- **에러 처리**
  - 문서에 명시적인 에러 처리 전략이 없음. 유일한 단서는 6장 `db.py` 예시가 예외 처리 없이 `supabase.table(...).execute()`를 그대로 호출한다는 점 — 즉 "에러 발생 시 어떻게 사용자에게 보여줄지"가 설계되어 있지 않음
  - 잠금된 IP의 응답 문구("잠긴 계정입니다")는 텍스트 매칭으로 검증(`"잠긴 계정" in res.text`)하도록 시뮬레이션 스크립트가 작성되어 있음 → 이는 **응답 형식이 구조화된 JSON이 아니라 최소한 부분적으로는 사람이 읽는 텍스트/HTML이어야 한다는 암묵적 제약**을 만듦(질문 4-5)

---

## 5. 위험요소

### 5-1. 중복 구현 위험
- `detector.py`(판정)와 `soar.py`(대응)가 별도 파일로 분리되어 있는데, 두 파일 모두 "실패 횟수 조회"를 각자 호출하면 동일한 Supabase 쿼리가 요청당 2회 이상 발생할 수 있음. `db.py`의 `count_recent_failures()`를 **단일 진입점으로 강제**하지 않으면 A·B와 C가 병렬 개발 중 로직을 각자 재구현할 가능성이 높음(4인 비전공자 팀, 병렬 작업 구조상 특히 취약).
- "잠금 여부 판단" 로직이 `app.py`(로그인 라우트에서 거부 응답), `detector.py`(판정), `soar.py`(실행) 세 곳에 걸쳐 있어 "잠금 조건"이 세 파일에 각각 하드코딩(임계값 5, 윈도우 60초)될 위험 — 상수를 한 곳(`config` 또는 `db.py` 상단)에 모으지 않으면 값이 어긋날 수 있음.

### 5-2. 정합성(consistency) 위험 — 가장 심각
- **잠금 상태를 저장할 테이블/컬럼이 스키마에 없음.** 현재 스키마(`login_attempts`)는 시도 로그만 저장하며 "이 IP는 언제부터 언제까지 잠겨 있다"는 상태를 표현할 곳이 없음.
  - 탐지 윈도우(60초)와 해제 시간(5분)이 다르기 때문에, 단순히 "최근 60초 내 5회 실패"만 체크하면 60초만 지나면 자동으로 다시 로그인 가능해져 "5분 후 자동 해제"라는 기획과 모순됨.
  - 관리자가 "즉시 해제" 버튼을 눌러도 되돌릴 잠금 상태가 DB에 없다면, 해제 로직이 실제로 무엇을 변경하는지 정의되지 않음(로그를 삭제? 별도 플래그를 세울?).
- 팀원 4명이 각자 로컬에서 서버를 띄우고 동시에 같은 Supabase 테이블을 바라보는 구조이므로, **여러 로컬 `app.py` 인스턴스가 동시에 같은 IP에 대해 판정/잠금을 수행하면 경쟁 조건(race condition)** 이 발생할 수 있음(예: 두 인스턴스가 동시에 5번째 실패를 감지해 Slack 알림이 중복 발송).

### 5-3. 권한(authorization) 위험
- `/dashboard`, 관리자 "즉시 해제" 버튼, `/api/status` 등 어디에도 **인증/인가 로직이 언급되지 않음**. 현재 설계대로면 이 엔드포인트에 접근 가능한 누구나(공격자 포함) 잠금을 즉시 해제할 수 있음 — SOAR 자동 대응의 실효성을 무력화하는 구조적 결함.
- 대시보드가 로그인 시도 로그(아이디 포함)를 그대로 노출하는데, 이 역시 접근 제어 없이 공개되면 사용자명 목록이 그대로 유출되는 정보노출 위험(캡스톤 시연용이라 리스크는 낮지만, 심사위원 Q&A에서 지적될 수 있는 지점).

### 5-4. 성능/쿼터 위험
- Supabase 무료 티어는 월 API 요청 약 5만 건 한도. 대시보드가 2~3초마다 폴링하고, 팀원 4명이 각자 로컬에서 동시에 개발 중 테스트를 반복하면 예상보다 빨리 한도에 도달할 수 있음(예: 1명이 대시보드를 30분 열어두면 약 600~900회 요청).
- `count_recent_failures`가 매 로그인 시도·매 폴링마다 전체 카운트 쿼리를 실행하는 구조라, 동시 접속자가 늘어나면 쿼리 수가 선형으로 증가 — 캡스톤 규모에서는 무해하지만, 인덱스(`ip_address`, `attempted_at`) 없이 운용하면 테이블이 커질 경우 느려질 수 있음(현재 DDL에 인덱스 없음).
- 시연용 브루트포스 시뮬레이터(`time.sleep(0.5)`)가 팀원 여러 명이 동시에 반복 실행하면 실패 로그가 급격히 쌓여 쿼터/대시보드 가독성에 영향을 줄 수 있음 — 별도 테스트 계정/IP 구분이 필요할 수 있음.

---

## 6. 구현 전 질문 (결정이 필요한 모호한 지점)

1. **잠금 상태를 어떻게 영속화할 것인가?**
   `login_attempts` 테이블만으로는 "현재 잠긴 IP와 잠금 해제 예정 시각"을 표현할 수 없습니다. 별도 `locked_ips`(또는 `lockouts`) 테이블을 추가할지, 아니면 `login_attempts`에 컬럼을 추가할지, 혹은 상태를 아예 저장하지 않고 매 요청마다 "최근 5분 내 실패 횟수 ≥ 5"로 재계산할지 결정이 필요합니다. (재계산 방식이면 60초 룰과 5분 룰 중 하나로 통일해야 함 — 문서 자체가 두 값을 혼용하고 있음.)

2. **"계정 잠금"인가 "IP 잠금"인가?**
   기획서 제목/다이어그램은 "계정 자동 잠금"이라 부르지만, 실제 판정·차단 로직은 전부 IP 기준입니다. 같은 사용자가 다른 IP로 시도하면 잠기지 않고, 같은 IP의 여러 계정 시도는 합산되어 잠기는 것이 의도인지 확인이 필요합니다.

3. **관리자 인증을 어느 수준까지 넣을 것인가?**
   `/dashboard`와 수동 해제 버튼에 로그인/비밀번호 같은 최소한의 보호를 넣을지, 캡스톤 시연 목적상 완전히 오픈해둘지 팀 결정이 필요합니다(6장 Q&A는 "실제 서비스라면 더 필요하다"는 답변만 준비되어 있고, 지금 구현 여부는 명시하지 않음).

4. **Slack 알림 채널을 어디에 걸 것인가(Incoming Webhook URL 발급 대상 워크스페이스/채널)**
   원 기획서는 텔레그램 봇 토큰 발급을 전제로 했으나, 팀 결정으로 Slack Incoming Webhook으로 대체합니다. `alert.py`는 `requests.post(webhook_url, json={"text": ...})` 한 줄로 충분해 라이브러리 선택 자체는 모호점이 사라지지만, **어느 Slack 워크스페이스·채널에 웹훅을 연결할지**(팀 개인 워크스페이스 신규 생성 여부 등)는 1~3일차에 팀이 정해야 합니다.

5. **잠긴 상태의 응답 형식**
   시뮬레이션 검증 코드는 `"잠긴 계정" in res.text`로 판단합니다. `/login`이 JSON을 반환할지, 서버 렌더링 HTML(플래시 메시지)을 반환할지에 따라 프런트(`login.html`)와 시뮬레이터(`bruteforce_sim.py`) 양쪽의 파싱 방식이 달라지므로 먼저 정해야 합니다.

6. **`/api/status`의 스펙**
   대시보드 JS가 폴링할 엔드포인트로 언급만 되고 응답 스키마(필드명, 잠금 목록 포함 여부, 페이지네이션 여부 등)가 없습니다. `dashboard.js`와 `app.py`를 동시에 담당자가 다르게 만들기 전에 계약(JSON 스키마)을 먼저 합의해야 합니다.

7. **IP 판별 기준**
   로컬 테스트 환경(127.0.0.1)에서는 팀원 전원이 같은 IP로 잡힐 가능성이 큽니다. 시연 시 서로 다른 "공격자"를 구분해야 한다면 `X-Forwarded-For` 헤더나 시뮬레이터에서 가짜 IP를 주입하는 방식이 필요한지 확인이 필요합니다(Flask 기본 `request.remote_addr`는 로컬 실행 시 항상 `127.0.0.1`).

8. **`daily_report.py`(AI 로그 요약)의 최소 스펙**
   기획서에 "LLM 프롬프트 연동" 외 구체적 스펙(어떤 API, 어떤 주기, 어떤 출력 형식)이 없어 스트레치 항목 중 가장 정의가 부족합니다. 시간이 부족하면 가장 먼저 드롭될 가능성이 높으므로, 착수 여부를 핵심 기능(4~8일차) 완료 이후로 재확인하는 것이 합리적입니다.

---

## 7. 결론

문서 두 개는 **기획/역할분담/설명 대본**으로는 매우 상세하지만, 실제 코드 계약(테이블 스키마의 잠금 상태 표현, `/api/status` 응답 형식, 인증 여부, IP vs 계정 기준)은 아직 확정되지 않은 부분이 많습니다. 구현에 착수하기 전, 위 8개 질문 중 최소 1·2·3번(잠금 상태 저장 방식, IP/계정 기준, 관리자 인증 여부)은 팀 전체가 합의하고 시작하는 것을 권장합니다. 이 세 가지가 스키마와 라우트 설계에 직접적인 영향을 주기 때문에, 나중에 바꾸면 `db.py`, `app.py`, `detector.py`, `soar.py` 네 파일을 모두 다시 손봐야 합니다.
